# 🚀 Быстрый старт

## Запуск

Дважды кликните на `run.bat` или запустите:
```bash
python app.py
```

## Смена темы

1. Откройте `themes.json` - там 8 готовых тем
2. Выберите понравившуюся тему, скопируйте блок `colors`
3. Откройте `config.json` и замените блок `colors`
4. Перезапустите программу

### Примеры тем

| Тема | Описание |
|------|----------|
| **cyberpunk** | Неоновый киберпанк (по умолчанию) - голубое свечение |
| **matrix** | Матрица - зеленые символы на черном |
| **sunset** | Закат - оранжево-розовые тона |
| **minimal** | Минимализм - только белые контуры |
| **purple** | Фиолетовый - мягкие фиолетовые тона |
| **fire** | Огонь - красно-оранжевые цвета |
| **ocean** | Океан - голубые водные тона |
| **gaming** | Игровая - яркие RGB цвета |

## Настройка позиции

Откройте `config.json` и измените:

```json
{
  "position": "bottom"  // или "top", "left", "right"
}
```

## Настройка размера

```json
{
  "scale": 0.5,  // от 0.3 до 2.0
  "width": 1400,
  "height": 300
}
```

## Функции

- **Автоматическая смена раскладки** - при переключении языка Windows
- **Подсветка клавиш** - при нажатии загораются и плавно затухают
- **Затухание при бездействии** - через 5 секунд становится полупрозрачным
- **Всегда на переднем плане** - не мешает другим окнам

## Горячие клавиши

Программа не использует горячие клавиши, она только отображает нажатия.

## Проблемы?

- **Не видно текста** → Откройте `config.json`, измените `key_bg` на `#50202020`
- **Клавиши накладываются** → Уменьшите `scale` или увеличьте `width`
- **Не меняется раскладка** → Проверьте, что используете Windows
- **Не запускается** → Установите зависимости: `pip install -r requirements.txt`
